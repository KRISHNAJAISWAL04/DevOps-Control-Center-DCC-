version: "3.9"

services:
  dcc:
    build:
      context: .
      dockerfile: app/Dockerfile

    container_name: dcc

    ports:
      - "8000:8000"

    restart: unless-stopped

    env_file:
      - .env 
